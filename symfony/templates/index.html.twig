{% extends 'base.html.twig' %}

{% block body %}
    <div class="fluid-container row mx-0 mx-md-n5">
        <div class="col px-md-5 p-5 d-flex align-items-center justify-content-end">
            <div id="link-triangle">
                <a id="link-triangle-text" class="link-shape" href="{{ path('triangle')}}">Triangle</a>
            </div>
        </div>
        <div class="col px-md-5 p-5 d-flex align-items-center justify-content-start">
            <a class="link-shape" id="link-circle" href="{{ path('circle')}}">Circle</a>
        </div>
    </div>
{% endblock %}
